services:
  app:
    build: .
    container_name: service-app
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:///app/prisma/data/dev.db
      - NODE_ENV=production
    volumes:
      - ./prisma/data:/app/prisma/data:z
      - ./public/uploads:/app/public/uploads:z
      - ./public/archives:/app/public/archives:z
    restart: always
